name: Release Container

on:
  workflow_call:
    inputs:
      image-name:
        description: Image name without registry (defaults to github.repository)
        type: string
        required: false
        default: ""
      image-description:
        description: OCI org.opencontainers.image.description label value
        type: string
        required: false
        default: ""
      dockerfile:
        description: Path to the Dockerfile
        type: string
        required: false
        default: Dockerfile
      context:
        description: Docker build context path
        type: string
        required: false
        default: .
      dry-run:
        description: Build only — skip login and do not push the image to GHCR
        type: boolean
        required: false
        default: false

permissions:
  contents: read
  packages: write

jobs:
  build-push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: mockasort-studio/actions/.github/actions/push-container@main
        with:
          image-name: ${{ inputs.image-name }}
          image-description: ${{ inputs.image-description }}
          dockerfile: ${{ inputs.dockerfile }}
          context: ${{ inputs.context }}
          dry-run: ${{ inputs.dry-run }}
