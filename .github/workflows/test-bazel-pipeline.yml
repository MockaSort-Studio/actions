name: Test — Bazel pipeline

on:
  workflow_call:

jobs:
  test-build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: ./.github/actions/build-bazel
        with:
          targets: //tests/fixtures/bazel-workspace/...
